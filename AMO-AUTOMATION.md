# Воронка amoCRM + двусторонняя автоматика

Связывает наш статус-движок (`reg.py`: lead→registered→paid→ticket_issued→checked_in)
и pretix-билеты с воронкой amoCRM. Менеджер работает **внутри amoCRM** (бот `/paid` —
резерв; оба пути сходятся в одну идемпотентную выдачу билета).

Модуль: [`server/backend/amo_sync.py`](server/backend/amo_sync.py). Использует
`app.amo_request` (авто-refresh токена). `reg.py` не менялся — доп. колонки
(`amo_last_status_id`, `amo_forward_ts`) и доступ к БД живут в `amo_sync`.

## Воронка (id=10957730) — этапы оставлены как есть
Неразобранное(86154866) · Рег без оплаты(86154870) · Принимают решение(86154878) ·
проверить оплату(86326414) · **Успешно — оплачено(142, WIN)** · Закрыто(143, LOST).
Приход/неявка/возврат — в полях, не в этапах (после WIN этапа нет).

## FORWARD (наши события → amoCRM)
| Событие | Действие в amoCRM |
|---|---|
| Новая рег (сайт/бот) `api_lead` | этап 86154870 + поля (Тариф, Формат, Браслет, Город, TG, reg_id, Статус оплаты=Не оплачено) + теги + задача |
| Бот: показана СБП-ссылка `pay_package` | → 86154878, Статус оплаты=Ссылка показана, задача +2ч |
| Бот: «я оплатил» `i_paid` | → 86326414, Статус оплаты=Заявлена (не подтв.), задача +30мин |
| `/paid` или reverse-WIN | → 142, Статус оплаты=Оплачено, Дата оплаты |
| Билет выпущен | Статус билета=Выпущен, № заказа pretix, Браслет |
| Check-in (pretixSCAN/web) | Статус билета=Использован, Дата check-in, тег checked-in |
| Отмена/возврат | → 143 + причина отказа, тег refund + задача |

Среднее автодвижение: авто до «проверить оплату»; в WIN/LOST — только по деньгам/руке.
`safe_set_stage` никогда не двигает терминальные (142/143) сделки и не откатывает назад.

## REVERSE (менеджер в amoCRM → наши действия)
Вебхук `status_lead` → `POST https://sadaosato.pro/amo/lead-webhook?key=<AMO_WEBHOOK_SECRET>`
(зарегистрирован, id 47789658; nginx `/amo/` уже проксирует на бэкенд):
- **перетащил в 142 (WIN)** → найти участника (по `amocrm_lead_id`/телефону) →
  `set_status(paid)` → `tickets.issue_ticket` (создаёт оплаченный заказ pretix) →
  доставка билета в TG → отметка полей. **Выпуск сразу** (по решению владельца).
- **перетащил в 143 (LOST)** → `set_status(cancelled)`; если билет был выпущен — задача на возврат.
- промежуточные этапы — no-op.

Защита: секрет в URL + проверка `account[id]==33077174`; анти-эхо (наш forward не
триггерит обратное действие 120с); идемпотентный выпуск (guard `pretix_order_code`);
gate на предусловия (нет тарифа → задача «дозаполни», билет не выпускается).

## Поля сделки (созданы, id 1888729–1888751)
Тариф, Статус оплаты, Статус билета, № заказа pretix, Браслет, Формат, TG username,
TG id, Город, Дата оплаты, Дата check-in, reg_id (БД). amo_sync резолвит их по имени
(динамический кэш — без хардкода id).

## Env (`/etc/sato/amo.env`)
`AMO_WEBHOOK_SECRET` (секрет reverse-вебхука), `AMO_RESPONSIBLE_USER=13874042`.
Токен amoCRM — `/var/lib/sato/amo_tokens.json` (refresh в `app.py`).

## Осталось / на будущее
- Reconciliation-джоб (сверка pretix↔reg↔amo) + no_show батч после саммита — задел.
- 2-й менеджер: вынести `AMO_RESPONSIBLE_USER` под round-robin.
- amoJo (чат менеджера) — отдельный дормант-модуль, ждёт channel creds.
